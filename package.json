{
  "name": "canshine-profile",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run format:check"
  },
  "devDependencies": {
    "prettier": "^3.5.0"
  }
}
